<nav class='navigation'>
  <ul>
    <% data.pages.each do |link| %>
    <li><a href='/<%= link.first.gsub('_', '-') %>' title='<%= link.first.capitalize.gsub('_', ' ') %>'><%= link.first.capitalize.gsub('_', ' ') %></a></li>
    <% end %>
  </ul>
</nav>
